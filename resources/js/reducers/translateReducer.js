const defaultState = {
    lang: [],
    homePage: {

    }
}

export default function translateReducer(state = defaultState, action) {
    switch (action.type) {

        default:
            return state
    }
};
